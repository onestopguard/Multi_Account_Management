name: Build and Release

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    permissions:
      contents: write
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install xwin
        uses: typora/xwin@release
        with:
          version: latest

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

      - name: Build Tauri
        run: npm run tauri build
        env:
          XWIN_ARCH: x64

      - name: List output files
        run: |
          Get-ChildItem -Path src-tauri/target -Recurse -Filter "*.exe" | Select-Object FullName, Length

      - name: Upload artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: release
          path: |
            src-tauri/target/release/*.exe
            src-tauri/target/release/bundle/nsis/*.exe